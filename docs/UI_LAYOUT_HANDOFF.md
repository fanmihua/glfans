# UI 排版修改交接文档

更新日期：2026-09-14

## 交接目标

本分支汇总了 CP 搜索框、REPO 合集展示和 REPO 正文文章排版的已确认修改，供管理员对照初始 `main` 审阅、测试和合并。

本文以本地 `main` 的 `3f06f3408bedf29568d24d5db46e5d88533fba32` 为初始基线，以 `ui-layout-adjustments` 分支的代码提交 `1802365a20238c8465424390d34bf688397191b8` 为对比终点。代码提交如下：

1. `88dfa3a` — 调整搜索框、合集封面与文章图片排版
2. `1802365` — 完善 REPO 正文排版并记录专栏规则

建议管理员合并整个 `ui-layout-adjustments` 分支，以保留代码和规则文档。若只需代码提交，可依次挑选上面两个提交。

## 最终差异概览

相对初始 `main`，代码提交共涉及 5 个文件，净变化为 141 行新增、61 行删除：

| 文件 | 最终变化 | 影响范围 |
| --- | --- | --- |
| [`AGENTS.md`](../AGENTS.md) | 新增 REPO 正文排版及验证规则 | 后续开发约束 |
| [`src/features/cp/cp-page.css`](../src/features/cp/cp-page.css) | CP 搜索框圆角改为 0 | CP 页，桌面与移动端 |
| [`src/features/column/ArticleView.jsx`](../src/features/column/ArticleView.jsx) | 调整标题结构、文章效果与指定文章布局标识 | REPO 正文文章页 |
| [`src/magazine.css`](../src/magazine.css) | 调整合集封面、合集资料块和正文桌面/平板排版 | REPO 首页、合集页和正文页 |
| [`src/styles/mobile-article.css`](../src/styles/mobile-article.css) | 修正正文移动端图片及段落间距 | REPO 正文移动端 |

`ArticleDocument.jsx` 曾在开发过程中临时支持隐藏第一张正文图片，随后已恢复；相对 `main` 没有最终差异，第一张正文图片仍由原始文章 XML 正常渲染。

## 已实现行为

### CP 页

- 搜索框 `border-radius` 从 `24px` 改为 `0`。
- PC 与移动端共用 `.cp-search`，因此两端同步生效。

### REPO 合集页

- 合集入口卡片封面默认显示原始彩色。
- 卡片悬停仍保留轻微缩放，取消由黑白变彩色的滤镜变化。
- “宿敌恋人”合集资料信息块取消整体倾斜，图标、文字和分隔线水平显示。

### REPO 正文标题

- 合集标题和文章标题改为同一行、基线对齐，并保持不主动换行。
- 粉色手绘装饰线移动到文章标题内部，只跟随文章标题宽度。
- 响应式字号和间距分别适配桌面、平板与移动端。
- 标题区取消固定最小高度，按内容自然撑开；底部留白包含装饰线高度，使正文整体上移。
- `rival-lover/wine-ep06-07` 不再在标题区重复展示首张图片，首图保留在正文内容中。

### REPO 正文图文布局

- `rival-lover/wine-ep06-07` 增加 `article-magazine--paired` 页面标识。
- 该文章在桌面端使用等宽两栏：图片始终在左，文字始终在右，不再交替方向。
- 右侧文字块与左侧整组图片垂直居中，即两者中心落在同一水平线上；段落文本仍按原有方式排版。
- 连续图片顺序排列，取消倾斜、错位和负边距重叠。
- 桌面端连续图片使用 `gap: 10px`；平板和移动端通过高优先级 `:nth-child(n)` 规则清除旧负上边距，最终图片边界间距同样为 10px。
- 移动端保留图片在上、对应文字在下的单栏阅读顺序。

### REPO 正文图片和滚动效果

- `.article-magazine` 内正文图片和标题图片使用原始彩色，取消灰度与去饱和滤镜。
- 取消正文图片悬停缩放及滤镜变化。
- `ArticleView` 不再启用 `useArticleReveal`，因此图文区块不会随滚动淡入、平移、旋转、缩放或变色。
- 顶部阅读进度条仍正常随滚动更新。

### REPO 正文段落间距

- 桌面正文段落下间距从 `1.38em` 减少 12px，最终为 `calc(1.38em - 12px)`；16px 字号下浏览器计算值为 10.08px。
- 手机端正文段落下间距为 4px。
- 图片间距、段落间距和图文分组间距仍分别管理。

## 作用域说明

写入 `AGENTS.md` 的新增排版规则只约束 REPO 文专栏正文文章页，即 `#/column/:collectionSlug/:articleSlug` 及对应路径。样式以 `.article-view.article-magazine` 为主要作用域。

分支还包含用户此前单独确认的 CP 搜索框和 REPO 合集页改动。这些不是正文规则的扩展，而是同一分支中需要一并交接的独立 UI 修改。

当前左图右文的两栏布局通过文章标识精确限定到 `rival-lover/wine-ep06-07`。标题、正文图片彩色、滚动效果和正文段落间距应用于 REPO 正文文章模板。管理员若计划让后续文章也采用两栏布局，应将 `article-magazine--paired` 的启用条件改为内容配置或文章元数据，不建议继续追加硬编码 slug。

## 验证结果

已完成以下验证：

```bash
npm run build
node --test tests/*.test.mjs server/tests/*.test.mjs
git diff --check
```

结果：

- 生产构建成功。
- 197 项测试全部通过，0 失败、0 跳过。
- 差异格式检查通过。
- 项目 `package.json` 未配置 lint 命令，因此没有可运行的独立 lint 检查。
- 在浏览器中确认桌面端左图右文、文字与图片组居中、第一张正文图片存在、图片彩色且无滚动/悬停变化。
- 在 390px 移动端视口中测得连续图片实际边界间距为 10px，图片 `margin-top` 为 0。

## 管理员审阅重点

1. 打开 `/column/rival-lover/wine-ep06-07/?lang=zh#/column/rival-lover/wine-ep06-07`，检查标题、首图、左右两栏、10px 图片间距和文字垂直居中。
2. 切换到约 390px 宽度，确认图片在上、文字在下，第二张及后续图片之间均为 10px，且没有旧负边距造成的重叠。
3. 滚动全文并悬停图片，确认图片始终保持彩色、水平和静止；阅读进度条仍更新。
4. 打开 REPO 合集首页，确认全部合集卡片封面默认彩色；进入“宿敌恋人”合集确认资料信息块水平显示。
5. 打开 CP 页，确认搜索框为直角，并检查桌面与移动端输入、清除按钮和聚焦边框。
6. 抽查其他 REPO 正文文章，确认模板级标题、彩色图片、无滚动效果及段落间距符合预期。

## 合并方式

推荐合并整个本地分支：

```bash
git switch main
git merge --no-ff ui-layout-adjustments
```

如管理员只希望挑选现有代码提交：

```bash
git switch main
git cherry-pick 88dfa3a 1802365
```

合并后再次执行生产构建与完整测试。如果目标 `main` 已更新，应先基于最新 `main` 检查 `ArticleView.jsx`、`magazine.css` 和 `mobile-article.css` 的冲突与样式顺序；其中移动端 `:nth-child(n)` 的优先级用于覆盖旧的偶数图片负上边距，解决冲突时不能误删。

## 回退参考

- 整体合并后需要回退时，优先对合并提交执行 `git revert`。
- 逐个挑选提交后需要回退时，按相反顺序回退 `1802365` 和 `88dfa3a`。
- 不建议用强制重置处理已共享的管理员分支。
