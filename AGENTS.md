# 项目规范

- 2026-09-21 用户授权恢复除「和谐密语」以外此前关闭的已发布文章：Us EP07、EP08、EP12 恢复目录、正文、分享直链、相邻文章导航和共用 App 内容快照，移除服务器对应拦截；和谐密语继续隐藏；用户明确保留《宿敌恋人 EP09–10》未完成草稿隐藏。此条替代此前三篇 Us 文章的隐藏要求。

- 2026-09-20 用户指出考古档案年份目录桌面顶部留白过多、标题过低：主标题、搜索与日历入口应位于首屏上半部，装饰贴纸容器不得在 Grid 中占据空行并下推正文。保留首屏底部胶卷与连续高度响应；手机布局保持。

- 2026-09-20 用户授权恢复线上网站全部既有功能并发布：欢迎开屏、坑底文学、评论/投稿/点赞、电台、后台登录，以及百家饭的电台/超话/商店入口恢复；后台仍要求管理员身份。本条替代备案期间的网页功能限制。保留独立内容隐藏决定（和谐密语、Us EP07/EP08/EP12）和 App 当前审核范围，不夹带本地实验，不自动恢复每周任务。

- 2026-09-17 用户后续明确网页与 App 都恢复 REPO 和表情包，坑底文学不开放。最终公开考古档案（含播出日历）、百家饭、REPO、表情包与基本说明；电台、评论/投稿/点赞与后台继续关闭。此条替代本日先前只保留档案与百家饭的范围。

- 2026-09-17 用户授权建立 `codex/filing-preparation` 分支并部署备案准备版本：公开内容只保留考古档案、播出日历、百家饭及基本关于/权利说明；首页直接进入档案，隐藏欢迎开屏、文学评论投稿/点赞、REPO、表情包、电台与后台入口，百家饭不展示电台、超话及商店跳转。前端、旧链接、分享页和服务器公开接口同步限制；原始代码、资料、数据库与完整 main 保留用于恢复。发布不得夹带原开发目录实验内容；备案期间暂停 glfans 每周任务，防止主线自动发布覆盖精简版。

## 执行流程与完成标准

- 写代码后必须运行测试，失败必须自动修复。
- 完成时所有测试通过，无 lint 错误；项目未配置 lint 时须如实说明。
- 搜索文件使用 `rg`，不用 `grep`。
- 不要修改 `.env` 文件。
- 不要删除或修改本次任务目标文件夹以外的文件。

## REPO 文专栏二级目录卡片规则

### 适用范围

- 本节仅适用于 REPO 文专栏合集的二级目录页：`#/column/:collectionSlug`，及其对应的 `/column/:collectionSlug/` 路径。
- 文章入口卡片列表以 `.collection-collage + .article-index` 为样式作用域，适用于该列表中的全部 `.article-card`。
- 不适用于 REPO 专栏首页的合集入口卡片，也不适用于 `#/column/:collectionSlug/:articleSlug` 正文文章页。

### 默认样式与交互

- 二级目录内所有文章卡片图片默认统一使用去色样式：`grayscale(0.82) contrast(1.08)`。
- 不得按卡片序号、文章 slug 或合集 slug 为单张卡片单独恢复默认彩色；第二张及后续卡片必须与第一张保持一致。
- 鼠标悬停或键盘聚焦时可沿用现有的轻微恢复色彩和缩放效果：`grayscale(0.15) contrast(1.02)`、`scale(1.035)`。
- REPO 专栏首页合集封面和正文文章图片仍按各自规则默认显示彩色，不能将二级目录的去色样式扩散到这些页面。

### 验证

- 修改二级目录卡片样式后运行生产构建和项目测试，并在浏览器中抽查包含多张卡片的合集。
- 必须检查全部卡片的计算样式，尤其确认第二张及后续卡片默认滤镜均为 `grayscale(0.82) contrast(1.08)`，且悬停和键盘聚焦状态仍可用。

## REPO 文专栏正文文章排版规则

### 适用范围

- 本节仅适用于 REPO 文专栏的正文文章页：`#/column/:collectionSlug/:articleSlug`，及其对应的 `/column/:collectionSlug/:articleSlug/` 路径。
- 正文页面由 `src/features/column/ArticleView.jsx` 和 `ArticleDocument.jsx` 渲染，以 `.article-view.article-magazine` 为样式作用域。
- 不适用于 REPO 专栏首页、合集页、文章入口卡片，也不适用于考古档案、百家饭、坑底文学、表情包、电台等其他栏目。
- 修改共享样式或渲染器时必须限定正文文章作用域，避免将本节规则扩散到其他页面。新增正文文章和后续排版调整均遵循本节规则。

### 标题

- 合集标题与文章标题同一行并排、基线对齐，不主动换行；窄屏需适配字号和间距，避免横向溢出或截断标题。
- 文章标题保留粉色，粉色装饰线紧随文章标题下方，不横跨合集标题。
- 标题区按内容自然撑开，取消造成大块空白的固定最小高度；装饰线下方留白与标题区上方留白保持视觉一致，使正文紧随其后。
- 正文第一张图片保留，不在标题区重复作为装饰图片展示。

### 图文排列与对齐

- 桌面端每组图文固定为左图右文，不交替左右方向；以等宽两栏为基础。
- 右侧文字块与左侧对应图片或整组图片的垂直中心对齐，即左右两块的中心落在同一水平线上；不是将段落文字改为居中排字。
- 移动端采用图片在上、对应文字在下的单栏顺序，保留原文段落和图片的对应关系。
- 所有图片保持水平、原始宽高比；连续图片在同一栏内顺序排列，不倾斜、不重叠，不使用负边距制造拼贴效果。

### 间距

- 连续图片之间的实际上下间距统一为 **10px**，桌面、平板、移动端一致。该值是图片边界之间的最终间距，不能叠加 `gap` 与外边距，也不能被负上边距抵消。
- 正文段落间距采用此前减少 12px 后的最终值，不在每次修改时再次减去 12px：桌面为 `calc(1.38em - 12px)`（16px 字号时为 10.08px），手机端为 4px。
- 段落间距与图文分组之间的间距分别管理，不将此段落规则误用为行高或所有区块间距。

### 图片颜色与动态效果

- 正文图片默认展示原始彩色，不添加灰度、去饱和或其他改变原图色彩的滤镜。
- 取消图片及所在图文区块的滚动淡入、位移、旋转、缩放和变色效果；取消图片悬停时的缩放与滤镜变化。
- 正文图片正常随文档滚动，不通过滚动触发图片外观变化。阅读进度条属于阅读功能，不受此图片规则影响。

### 验证

- 涉及上述排版的代码变更后运行生产构建和项目测试；同时检查桌面与移动端实际布局。
- 图片间距必须通过浏览器元素边界测量确认是 10px，尤其检查第二张、第三张及后续图片的 `:nth-child` 规则是否覆盖移动端样式。
- 验证标题无换行或横向溢出、图文中心对齐、首张正文图片存在、图片无重叠且始终彩色、滚动与悬停不触发图片变化。

- 2026-09-17 用户确认网站和 App 同步暂时隐藏 Us REPO 的 EP07、EP08、EP12：保留原稿与素材，其余 REPO 继续开放；目录、数量、上一篇/下一篇、直达入口及 App 内容快照/对应译文统一排除。用户要求审核说明明确免费、非盈利，并尝试正常审核；不得将非盈利说明当成已获第三方素材授权声明。
